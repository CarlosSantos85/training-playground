pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
     stage('Create File') {
            steps {
                sh 'touch /path/to/file.txt'
            }
        }
    }
}
