pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
     stage('Create File') {
            steps {
                sh touch /file.txt
                sh echo $(date) > /file.txt               
		sleep 10s
            }
        }
    }
}
